# Exported/reusable — consuming repos call this via
# uses: infinitetoken/Workflows/.github/workflows/npm-ci.yml@v1
# See examples/npm-ci.yml for a copy-pasteable caller.
name: npm-ci

on:
  workflow_call:
    inputs:
      node-version:
        description: Node.js version to use
        type: string
        default: '22'
      script:
        description: npm script to run after install
        type: string
        default: 'verify'

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: npm

      - run: npm ci

      - name: tsconfig drift check
        continue-on-error: true
        run: |
          curl -s https://raw.githubusercontent.com/infinitetoken/Workflows/main/scripts/check-tsconfig-drift.cjs -o ./.check-tsconfig-drift.cjs
          node ./.check-tsconfig-drift.cjs
          rm -f ./.check-tsconfig-drift.cjs

      - run: npm run ${{ inputs.script }}
