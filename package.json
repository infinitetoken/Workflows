{
  "name": "workflows",
  "version": "1.0.3",
  "private": true,
  "description": "Shared, reusable GitHub Actions workflows for InfiniteToken repos",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/infinitetoken/Workflows.git"
  },
  "license": "MIT",
  "author": "Jay Deaton",
  "type": "commonjs",
  "scripts": {
    "fix": "eslint . --fix",
    "lint": "eslint . && actionlint .github/workflows/*.yml examples/*.yml && shellcheck scripts/*.sh",
    "release": "git push --follow-tags && bash scripts/move-major-tag.sh",
    "release:major": "npm version major && npm run release",
    "release:minor": "npm version minor && npm run release",
    "release:patch": "npm version patch && npm run release",
    "preversion": "npm run lint"
  },
  "prettier": "@infinitetoken/eslint-config/prettier",
  "devDependencies": {
    "@infinitetoken/eslint-config": "^0.1.4",
    "eslint": "^9.39.4"
  }
}
